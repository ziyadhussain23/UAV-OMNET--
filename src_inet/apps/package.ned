//
// Maps the NED package `apps` onto the C++ namespace the modules are defined
// in, mirroring src/nodes/package.ned's convention for the non-INET track.
//
package apps;
@namespace(uavauth::inet_apps);
