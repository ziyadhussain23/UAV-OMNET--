package nodes;

//
// Build/link smoke check. Verifies the canonical TLV encoder and OpenSSL are
// both reachable from inside the simulation binary.
//
simple SmokeNode
{
    @display("i=block/cogwheel");
}
