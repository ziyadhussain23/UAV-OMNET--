//
// Maps the NED package `nodes` onto the C++ namespace the modules are defined
// in, so Define_Module() registrations resolve without an @class() on every
// simple module.
//
package nodes;
@namespace(uavauth::nodes);
